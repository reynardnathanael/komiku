import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'komiku_160720034',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
